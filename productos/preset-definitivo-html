<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Preset Definitivo - OFFSZN Academy</title>
    <link rel="stylesheet" href="../css/style.css">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600;700;800;900&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

    <!-- GSAP + ScrollTrigger (LOCAL) -->
    <script src="../libs/gsap/gsap.min.js"></script>
    <script src="../libs/gsap/ScrollTrigger.min.js"></script>

    <!-- Lenis Smooth Scroll (LOCAL) -->
    <script src="../libs/lenis/lenis.min.js"></script>

    <style>
        /* ===== VARIABLES PREMIUM ===== */
        :root {
            --primary: #7209b7;
            --primary-light: #c9a7eb;
            --primary-dark: #560bad;
            --accent: #b794f6;
            --bg-dark: #000;
            --bg-card: rgba(26, 10, 46, 0.4);
            --border: rgba(114, 9, 183, 0.3);
            --text-light: #e9d5ff;
            --text-muted: #d1d1d1;
            --gradient: linear-gradient(135deg, #7209b7, #560bad);
            --glow: 0 0 30px rgba(114, 9, 183, 0.4);
        }

        /* ===== SMOOTH SCROLL ===== */
        html.lenis {
            height: auto;
        }

        .lenis.lenis-smooth {
            scroll-behavior: auto;
        }

        .lenis.lenis-smooth [data-lenis-prevent] {
            overscroll-behavior: contain;
        }

        .lenis.lenis-stopped {
            overflow: hidden;
        }

        .lenis.lenis-scrolling iframe {
            pointer-events: none;
        }

        /* ===== HERO SECTION - DISEÑO EXCLUSIVO ===== */
        .preset-detail-hero {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 100px 8% 80px;
            gap: 80px;
            min-height: 90vh;
            background: 
                radial-gradient(circle at 20% 80%, rgba(114, 9, 183, 0.15), transparent 40%),
                radial-gradient(circle at 80% 20%, rgba(86, 11, 173, 0.12), transparent 50%),
                linear-gradient(180deg, #000 0%, #0a0118 100%);
            position: relative;
            overflow: hidden;
        }

        .preset-detail-hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg"><defs><pattern id="grid" width="100" height="100" patternUnits="userSpaceOnUse"><path d="M 100 0 L 0 0 0 100" fill="none" stroke="rgba(114,9,183,0.03)" stroke-width="1"/></pattern></defs><rect width="100%" height="100%" fill="url(%23grid)"/></svg>');
            opacity: 0.4;
            pointer-events: none;
        }

        .preset-detail-content {
            max-width: 650px;
            flex: 1;
            z-index: 2;
        }

        .preset-badge {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: linear-gradient(135deg, rgba(114, 9, 183, 0.3), rgba(86, 11, 173, 0.2));
            backdrop-filter: blur(10px);
            border: 1px solid rgba(114, 9, 183, 0.6);
            padding: 12px 24px;
            border-radius: 50px;
            margin-bottom: 32px;
            color: var(--primary-light);
            font-weight: 700;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            box-shadow: 0 8px 32px rgba(114, 9, 183, 0.2);
            position: relative;
            overflow: hidden;
        }

        .preset-badge::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transition: left 0.5s;
        }

        .preset-badge:hover::before {
            left: 100%;
        }

        .preset-badge i {
            font-size: 16px;
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.2); opacity: 0.8; }
        }

        .preset-detail-title {
            font-size: 72px;
            font-weight: 900;
            color: #fff;
            margin-bottom: 24px;
            line-height: 1.1;
            font-family: 'Montserrat', sans-serif;
            text-shadow: 0 0 40px rgba(114, 9, 183, 0.5);
            letter-spacing: -2px;
        }

        .highlight-gradient {
            background: linear-gradient(135deg, #7209b7, #b794f6, #c9a7eb);
            background-size: 200% auto;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: shimmer 3s linear infinite;
            filter: drop-shadow(0 0 20px rgba(114, 9, 183, 0.5));
        }

        @keyframes shimmer {
            to { background-position: 200% center; }
        }

        .preset-detail-subtitle {
            font-size: 18px;
            line-height: 1.8;
            color: var(--text-muted);
            margin-bottom: 40px;
            font-weight: 400;
        }

        .hero-cta-group {
            display: flex;
            gap: 20px;
            margin-bottom: 50px;
            flex-wrap: wrap;
        }

        .btn {
            position: relative;
            overflow: hidden;
            padding: 20px 48px;
            font-size: 16px;
            font-weight: 700;
            border-radius: 12px;
            display: inline-flex;
            align-items: center;
            gap: 12px;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            box-shadow: 0 8px 30px rgba(114, 9, 183, 0.4);
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 12px 40px rgba(114, 9, 183, 0.6);
        }

        .btn i {
            font-size: 18px;
            transition: transform 0.3s;
        }

        .btn:hover i {
            transform: translateX(5px);
        }

        .btn-secondary {
            background: transparent;
            border: 2px solid rgba(114, 9, 183, 0.6);
            box-shadow: 0 4px 20px rgba(114, 9, 183, 0.2);
        }

        .btn-secondary:hover {
            background: rgba(114, 9, 183, 0.15);
            border-color: rgba(201, 167, 235, 0.9);
        }

        /* ===== IMAGEN HERO CON EFECTOS PREMIUM ===== */
        .preset-image-container {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            min-width: 450px;
            position: relative;
        }

        .preset-image-placeholder {
            width: 100%;
            max-width: 550px;
            height: 450px;
            background: linear-gradient(135deg, rgba(26, 10, 46, 0.6), rgba(10, 1, 24, 0.4));
            backdrop-filter: blur(20px);
            border: 2px solid rgba(114, 9, 183, 0.4);
            border-radius: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            color: var(--primary-light);
            font-size: 20px;
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
            box-shadow: 
                0 20px 60px rgba(114, 9, 183, 0.3),
                inset 0 0 60px rgba(114, 9, 183, 0.1);
        }

        .preset-image-placeholder::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(from 0deg, transparent, rgba(114, 9, 183, 0.3), transparent 30%);
            animation: rotate 4s linear infinite;
        }

        @keyframes rotate {
            100% { transform: rotate(360deg); }
        }

        .preset-image-placeholder::after {
            content: '';
            position: absolute;
            inset: 3px;
            background: #000;
            border-radius: 22px;
            z-index: 1;
        }

        .preset-image-placeholder i,
        .preset-image-placeholder p {
            position: relative;
            z-index: 2;
        }

        .preset-image-placeholder:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 
                0 30px 80px rgba(114, 9, 183, 0.5),
                inset 0 0 80px rgba(114, 9, 183, 0.2);
        }

        /* ===== SECCIONES CON GLASSMORPHISM ===== */
        .video-section,
        .before-after-section,
        .features-section,
        .pricing-section,
        .bonus-section,
        .faq-section,
        .reviews-section {
            padding: 100px 6%;
            position: relative;
        }

        .video-section {
            background: linear-gradient(180deg, rgba(10, 1, 24, 0.4), rgba(26, 10, 46, 0.3));
        }

        .section-header-center {
            text-align: center;
            margin-bottom: 80px;
        }

        .section-eyebrow {
            font-size: 12px;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: var(--accent);
            display: inline-block;
            margin-bottom: 16px;
            padding: 8px 20px;
            background: rgba(114, 9, 183, 0.15);
            border-radius: 30px;
            backdrop-filter: blur(10px);
        }

        .section-header-center h2 {
            font-size: 52px;
            font-weight: 900;
            color: #fff;
            margin-bottom: 20px;
            font-family: 'Montserrat', sans-serif;
            text-shadow: 0 0 40px rgba(114, 9, 183, 0.4);
            letter-spacing: -1px;
        }

        .section-header-center p {
            font-size: 18px;
            color: var(--text-muted);
            max-width: 700px;
            margin: 0 auto;
        }

        /* ===== VIDEO CONTAINER PREMIUM ===== */
        .video-container {
            max-width: 1000px;
            margin: 0 auto;
            aspect-ratio: 16/9;
            background: linear-gradient(135deg, rgba(26, 10, 46, 0.6), rgba(10, 1, 24, 0.5));
            backdrop-filter: blur(20px);
            border: 2px solid rgba(114, 9, 183, 0.4);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(114, 9, 183, 0.3);
            transition: all 0.4s;
        }

        .video-container:hover {
            transform: translateY(-8px);
            box-shadow: 0 30px 80px rgba(114, 9, 183, 0.4);
            border-color: rgba(201, 167, 235, 0.6);
        }

        /* ===== CARDS CON MICRO-INTERACCIONES ===== */
        .ba-card,
        .feature-card,
        .bonus-card,
        .review-card {
            background: linear-gradient(135deg, rgba(26, 10, 46, 0.6), rgba(10, 1, 24, 0.4));
            backdrop-filter: blur(15px);
            border: 1px solid rgba(114, 9, 183, 0.3);
            border-radius: 16px;
            padding: 32px;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
        }

        .ba-card::before,
        .feature-card::before,
        .bonus-card::before,
        .review-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(114, 9, 183, 0.1), transparent);
            transition: left 0.6s;
        }

        .ba-card:hover::before,
        .feature-card:hover::before,
        .bonus-card:hover::before,
        .review-card:hover::before {
            left: 100%;
        }

        .ba-card:hover,
        .feature-card:hover,
        .bonus-card:hover,
        .review-card:hover {
            transform: translateY(-12px);
            border-color: rgba(201, 167, 235, 0.7);
            box-shadow: 0 20px 60px rgba(114, 9, 183, 0.3);
        }

        /* ===== FEATURE ICONS ANIMADOS ===== */
        .feature-icon {
            font-size: 48px;
            color: var(--primary);
            margin-bottom: 20px;
            display: inline-block;
            padding: 20px;
            background: rgba(114, 9, 183, 0.15);
            border-radius: 16px;
            transition: all 0.4s;
        }

        .feature-card:hover .feature-icon {
            transform: scale(1.1) rotateY(360deg);
            background: rgba(114, 9, 183, 0.25);
            box-shadow: 0 0 30px rgba(114, 9, 183, 0.4);
        }

        /* ===== PRICING CARD DESTACADO ===== */
        .pricing-card-preset {
            background: linear-gradient(135deg, rgba(26, 10, 46, 0.7), rgba(10, 1, 24, 0.5));
            backdrop-filter: blur(20px);
            border: 2px solid rgba(114, 9, 183, 0.5);
            border-radius: 24px;
            padding: 48px 36px;
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
        }

        .pricing-card-preset::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, #7209b7, #560bad, #7209b7);
            background-size: 300% 300%;
            border-radius: 24px;
            z-index: -1;
            animation: gradient-animation 3s ease infinite;
            opacity: 0;
            transition: opacity 0.4s;
        }

        @keyframes gradient-animation {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .pricing-card-preset:hover::before {
            opacity: 1;
        }

        .pricing-card-preset:hover {
            transform: translateY(-16px) scale(1.02);
            box-shadow: 0 30px 80px rgba(114, 9, 183, 0.5);
        }

        .pricing-card-preset.featured {
            border-color: var(--primary);
            border-width: 3px;
            box-shadow: 0 20px 60px rgba(114, 9, 183, 0.4);
        }

        .featured-badge {
            position: absolute;
            top: -16px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--gradient);
            color: #fff;
            padding: 8px 28px;
            border-radius: 30px;
            font-size: 12px;
            font-weight: 800;
            letter-spacing: 1px;
            text-transform: uppercase;
            box-shadow: 0 8px 24px rgba(114, 9, 183, 0.5);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .featured-badge i {
            animation: sparkle 1.5s ease-in-out infinite;
        }

        @keyframes sparkle {
            0%, 100% { transform: scale(1) rotate(0deg); }
            50% { transform: scale(1.2) rotate(180deg); }
        }

        /* ===== FAQ ACCORDION MEJORADO ===== */
        .faq-item-preset {
            background: linear-gradient(135deg, rgba(26, 10, 46, 0.6), rgba(10, 1, 24, 0.4));
            backdrop-filter: blur(15px);
            border-radius: 16px;
            margin-bottom: 20px;
            overflow: hidden;
            transition: all 0.4s;
            cursor: pointer;
            border: 1px solid rgba(114, 9, 183, 0.3);
        }

        .faq-item-preset:hover {
            border-color: rgba(201, 167, 235, 0.6);
            box-shadow: 0 8px 32px rgba(114, 9, 183, 0.2);
        }

        .faq-question {
            padding: 24px 28px;
            font-weight: 600;
            color: var(--text-light);
            position: relative;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s;
        }

        .faq-question::after {
            content: '+';
            font-size: 28px;
            font-weight: bold;
            color: var(--primary);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .faq-item-preset.active .faq-question {
            background: rgba(114, 9, 183, 0.1);
        }

        .faq-item-preset.active .faq-question::after {
            content: '−';
            transform: rotate(180deg);
        }

        .faq-answer {
            max-height: 0;
            padding: 0 28px;
            color: var(--text-muted);
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .faq-item-preset.active .faq-answer {
            padding: 0 28px 24px;
            max-height: 800px;
        }

        /* ===== REVIEWS MEJORADOS ===== */
        .review-card {
            padding: 32px 28px;
        }

        .reviewer-avatar {
            width: 56px;
            height: 56px;
            border-radius: 50%;
            background: var(--gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-weight: 800;
            font-size: 20px;
            box-shadow: 0 8px 24px rgba(114, 9, 183, 0.4);
            position: relative;
        }

        .reviewer-avatar::before {
            content: '';
            position: absolute;
            inset: -4px;
            border-radius: 50%;
            background: var(--gradient);
            opacity: 0.3;
            animation: pulse-ring 2s ease-out infinite;
        }

        @keyframes pulse-ring {
            0% { transform: scale(1); opacity: 0.3; }
            100% { transform: scale(1.4); opacity: 0; }
        }

        /* ===== COUNTDOWN TIMER ===== */
        .countdown-timer-preset {
            text-align: center;
            margin-top: 40px;
            padding: 28px;
            background: linear-gradient(135deg, rgba(114, 9, 183, 0.2), rgba(86, 11, 173, 0.1));
            backdrop-filter: blur(10px);
            border-radius: 16px;
            border: 1px solid rgba(114, 9, 183, 0.3);
        }

        .timer-display {
            display: flex;
            justify-content: center;
            gap: 24px;
        }

        .timer-block {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 16px 20px;
            background: rgba(0, 0, 0, 0.5);
            border-radius: 12px;
            border: 1px solid rgba(114, 9, 183, 0.4);
            min-width: 80px;
        }

        .timer-number {
            font-size: 42px;
            font-weight: 900;
            color: var(--primary-light);
            font-family: 'Montserrat', sans-serif;
            line-height: 1;
            text-shadow: 0 0 20px rgba(114, 9, 183, 0.6);
        }

        .timer-label {
            font-size: 11px;
            color: #999;
            text-transform: uppercase;
            margin-top: 8px;
            letter-spacing: 1px;
            font-weight: 600;
        }

        /* ===== RESPONSIVE ===== */
        @media (max-width: 900px) {
            .preset-detail-hero {
                flex-direction: column;
                padding: 60px 5%;
                gap: 50px;
                min-height: auto;
            }

            .preset-detail-title {
                font-size: 48px;
            }

            .preset-image-container {
                min-width: unset;
                width: 100%;
            }

            .section-header-center h2 {
                font-size: 36px;
            }

            .timer-display {
                gap: 16px;
            }

            .timer-block {
                min-width: 70px;
                padding: 12px 16px;
            }

            .timer-number {
                font-size: 32px;
            }
        }

        @media (max-width: 600px) {
            .preset-detail-title {
                font-size: 36px;
            }

            .section-header-center h2 {
                font-size: 28px;
            }

            .btn {
                width: 100%;
                justify-content: center;
            }

            .timer-display {
                gap: 12px;
            }

            .timer-block {
                min-width: 60px;
                padding: 10px 12px;
            }

            .timer-number {
                font-size: 24px;
            }
        }

        /* ===== ESTILOS ADICIONALES DE TU CSS ===== */
        .ba-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .ba-label {
            font-size: 14px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .ba-label.before { color: #f0483e; }
        .ba-label.after { color: #0cbc87; }

        .waveform-placeholder {
            width: 100%;
            height: 150px;
            background: rgba(0, 0, 0, 0.5);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #999;
            font-size: 14px;
            margin-bottom: 20px;
        }

        .audio-player {
            width: 100%;
            margin-top: 10px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 32px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .pricing-options {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(420px, 1fr));
            gap: 48px;
            margin-top: 50px;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
        }

        .pricing-title-preset {
            font-size: 28px;
            font-weight: 800;
            color: #fff;
            text-align: center;
            margin-bottom: 24px;
        }

        .pricing-price-preset {
            text-align: center;
            margin-bottom: 24px;
        }

        .price-amount-preset {
            font-size: 56px;
            font-weight: 900;
            color: var(--primary-light);
            font-family: 'Montserrat', sans-serif;
        }

        .price-original-preset {
            font-size: 22px;
            color: #999;
            text-decoration: line-through;
            margin-left: 12px;
        }

        .pricing-includes {
            list-style: none;
            margin: 32px 0;
            padding: 0;
        }

        .pricing-includes li {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 14px 0;
            color: var(--text-muted);
            font-size: 15px;
            border-bottom: 1px solid rgba(114, 9, 183, 0.1);
        }

        .pricing-includes i {
            color: var(--primary);
            font-size: 18px;
            flex-shrink: 0;
        }

        .btn-block {
            width: 100%;
            justify-content: center;
            margin-bottom: 24px;
        }

        .bonus-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 32px;
            max-width: 1100px;
            margin: 50px auto 0;
        }

        .bonus-badge-card {
            display: inline-block;
            background: #0cbc87;
            color: #fff;
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            margin-bottom: 16px;
            box-shadow: 0 4px 16px rgba(12, 188, 135, 0.3);
        }

        .bonus-value {
            color: #999;
            text-decoration: line-through;
            font-size: 14px;
            margin-left: 8px;
        }

        .bonus-card h3 {
            font-size: 22px;
            font-weight: 700;
            color: #fff;
            margin-bottom: 16px;
        }

        .bonus-description {
            color: var(--text-muted);
            font-size: 15px;
            line-height: 1.7;
        }

        .faq-container {
            max-width: 900px;
            margin: 0 auto;
        }

        .reviews-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
            gap: 32px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .review-header {
            display: flex;
            align-items: center;
            gap: 16px;
            margin-bottom: 20px;
        }

        .reviewer-info h4 {
            color: #fff;
            font-size: 17px;
            font-weight: 600;
            margin-bottom: 6px;
        }

        .review-date {
            color: #999;
            font-size: 13px;
        }

        .review-rating {
            color: #ffc107;
            font-size: 16px;
            margin-bottom: 16px;
        }

        .review-title {
            color: var(--primary-light);
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 12px;
        }

        .review-text {
            color: var(--text-muted);
            font-size: 14px;
            line-height: 1.7;
        }

        .overall-rating {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 16px;
            margin-top: 24px;
        }

        .rating-stars {
            color: #ffc107;
            font-size: 28px;
        }

        .rating-count {
            color: var(--text-muted);
            font-size: 15px;
        }

        @media (max-width: 900px) {
            .ba-grid {
                grid-template-columns: 1fr;
            }

            .pricing-options {
                grid-template-columns: 1fr;
            }

            .pricing-card-preset.featured {
                transform: scale(1);
            }
        }
    </style>
</head>

<body>
    <!-- ==================== NAVBAR ==================== -->
    <header class="navbar" id="navbar">
        <div class="logo">
            <a href="../index.html">OFFSZN</a>
        </div>

        <button class="menu-toggle" id="menuToggle" aria-label="Menú">
            <i class="bi bi-list"></i>
        </button>

        <nav id="navMenu">
            <button class="menu-close-btn" id="menuCloseBtn" aria-label="Cerrar menú">×</button>
            <ul id="navbarLinks">
                <li><a href="../index.html">Inicio</a></li>
                <li><a href="cursos.html">Cursos</a></li>
                <li><a href="presets-v2.html" class="active">Presets</a></li>
                <li><a href="playlist.html">Playlist</a></li>
                <li><a href="contacto.html">Contacto</a></li>
            </ul>
        </nav>

        <div class="user-actions">
            <a href="cart.html" class="cart-icon navbar-icon-button" title="Carrito">
                <i class="bi bi-cart3"></i>
            </a>
            <a href="login.html" class="login-icon logged-out navbar-icon-button" title="Iniciar Sesión">
                <i class="bi bi-person"></i>
            </a>
            <a href="my-products.html" class="account-icon logged-in navbar-icon-button" title="Mi Cuenta">
                <i class="bi bi-person-check"></i>
            </a>
            <button id="global-logout-button" class="logout-button logged-in navbar-icon-button" title="Cerrar Sesión">
                <i class="bi bi-box-arrow-right"></i>
            </button>
        </div>
    </header>

    <!-- ==================== HERO SECTION ==================== -->
    <section class="preset-detail-hero">
        <div class="preset-detail-content">
            <div class="preset-badge">
                <i class="bi bi-lightning-charge-fill"></i>
                Mezcla Profesional
            </div>
            <h1 class="preset-detail-title">
                Preset <span class="highlight-gradient">Definitivo</span>
            </h1>
            <p class="preset-detail-subtitle">
                Transforma tus voces al instante con nuestro preset profesional, creando mezclas inmersivas y de calidad de estudio. Compatible con FL Studio y optimizado para géneros urbanos.
            </p>
            <div class="hero-cta-group">
                <a href="#pricing" class="btn">
                    Obtener Preset Definitivo
                    <i class="bi bi-arrow-right"></i>
                </a>
                <a href="#video" class="btn btn-secondary">
                    <i class="bi bi-play-circle"></i>
                    Ver Demo
                </a>
            </div>
        </div>

        <div class="preset-image-container">
            <div class="preset-image-placeholder">
                <i class="bi bi-image" style="font-size: 64px;"></i>
                <p style="margin-top: 20px; font-weight: 600;">Imagen del Preset</p>
            </div>
        </div>
    </section>

    <!-- ==================== VIDEO SECTION ==================== -->
    <section id="video" class="video-section">
        <div class="section-header-center">
            <span class="section-eyebrow">Ve el Preset en Acción</span>
            <h2>Tutorial Completo del Preset Definitivo</h2>
            <p>Aprende a usar cada parámetro y plugin para obtener los mejores resultados</p>
        </div>

        <div class="video-container">
            <div class="video-placeholder">
                <div style="text-align: center;">
                    <i class="bi bi-play-circle-fill" style="font-size: 72px; color: #7209b7; margin-bottom: 20px;"></i>
                    <p style="font-size: 18px; color: #c9a7eb; margin-bottom: 8px;">Espacio para video de YouTube</p>
                    <p style="font-size: 14px; color: #999;">
                        https://www.youtube.com/watch?v=S_XeQD-SgoA
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- ==================== BEFORE/AFTER SECTION ==================== -->
    <section class="before-after-section">
        <div class="section-header-center">
            <span class="section-eyebrow">Escucha la Diferencia</span>
            <h2>Antes y Después del Preset</h2>
            <p>Compara la calidad de tu audio antes y después de aplicar el Preset Definitivo</p>
        </div>

        <div class="ba-grid">
            <div class="ba-card">
                <div class="ba-label before">
                    <i class="bi bi-x-circle"></i>
                    ANTES
                </div>
                <div class="waveform-placeholder">
                    <p>Espacio para visualización de onda de audio</p>
                </div>
                <audio class="audio-player" controls>
                    <source src="" type="audio/mpeg">
                    Tu navegador no soporta el elemento de audio.
                </audio>
                <p style="color: #999; font-size: 13px; margin-top: 12px; text-align: center;">
                    Audio sin procesar
                </p>
            </div>

            <div class="ba-card">
                <div class="ba-label after">
                    <i class="bi bi-check-circle"></i>
                    DESPUÉS
                </div>
                <div class="waveform-placeholder">
                    <p>Espacio para visualización de onda de audio</p>
                </div>
                <audio class="audio-player" controls>
                    <source src="" type="audio/mpeg">
                    Tu navegador no soporta el elemento de audio.
                </audio>
                <p style="color: #0cbc87; font-size: 13px; margin-top: 12px; text-align: center;">
                    Audio con Preset Definitivo aplicado
                </p>
            </div>
        </div>
    </section>

    <!-- ==================== FEATURES SECTION ==================== -->
    <section class="features-section">
        <div class="section-header-center">
            <span class="section-eyebrow">¿Qué Hace el Preset Definitivo?</span>
            <h2>Características Profesionales</h2>
            <p>Todo lo que necesitas para llevar tus voces al siguiente nivel</p>
        </div>

        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="bi bi-sliders"></i>
                </div>
                <h3>Compresión Profesional</h3>
                <p>Controla la dinámica de tus voces sin perder naturalidad. Balance perfecto entre presencia y claridad.</p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="bi bi-soundwave"></i>
                </div>
                <h3>Ecualización Precisa</h3>
                <p>Resalta las frecuencias correctas y elimina las que sobran. Tus voces se escucharán claras en cualquier sistema.</p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="bi bi-stars"></i>
                </div>
                <h3>Balance Frecuencial</h3>
                <p>Equilibrio perfecto diseñado específicamente para géneros urbanos: trap, drill, plugg, reggaetón y R&B.</p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="bi bi-cpu"></i>
                </div>
                <h3>Plugins WAVES</h3>
                <p>Utiliza herramientas profesionales de la industria musical. Calidad de estudio garantizada.</p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="bi bi-file-earmark-play"></i>
                </div>
                <h3>Video Tutorial Incluido</h3>
                <p>Aprende exactamente qué plugins usar y cómo configurar cada parámetro para obtener resultados óptimos.</p>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="bi bi-lightning-charge-fill"></i>
                </div>
                <h3>Resultados Instantáneos</h3>
                <p>Solo carga el preset y obtén sonido de estudio al instante. Perfecto para productores de cualquier nivel.</p>
            </div>
        </div>
    </section>

    <!-- ==================== PRICING SECTION ==================== -->
    <section id="pricing" class="pricing-section">
        <div class="section-header-center">
            <span class="section-eyebrow">Elige Tu Opción</span>
            <h2>Opciones de Compra</h2>
            <p>Selecciona la opción que mejor se adapte a tus necesidades</p>
        </div>

        <div class="pricing-container">
            <div class="pricing-options">
                <!-- OPCIÓN 1 -->
                <div class="pricing-card-preset">
                    <h3 class="pricing-title-preset">Preset Definitivo</h3>
                    <div class="pricing-price-preset">
                        <span class="price-amount-preset">$15</span>
                        <span style="font-size: 18px; color: #d1d1d1;">USD</span>
                    </div>

                    <ul class="pricing-includes">
                        <li><i class="bi bi-check-circle-fill"></i> Preset Definitivo para FL Studio</li>
                        <li><i class="bi bi-check-circle-fill"></i> Mezcla de Voz Principal</li>
                        <li><i class="bi bi-check-circle-fill"></i> Mezcla de BUS de Voces</li>
                        <li><i class="bi bi-check-circle-fill"></i> Video Tutorial de Plugins y Parámetros</li>
                        <li><i class="bi bi-check-circle-fill"></i> Compatible con FL Studio</li>
                        <li><i class="bi bi-check-circle-fill"></i> Plugins de WAVES incluidos</li>
                    </ul>

                    <a href="#" class="btn btn-block">
                        Comprar Ahora
                        <i class="bi bi-arrow-right"></i>
                    </a>
                </div>

                <!-- OPCIÓN 2 -->
                <div class="pricing-card-preset featured">
                    <div class="featured-badge">
                        <i class="bi bi-star-fill"></i>
                        Edición de Lanzamiento
                    </div>
                    <h3 class="pricing-title-preset">Preset Definitivo - Edición de Lanzamiento</h3>
                    <div class="pricing-price-preset">
                        <span class="price-amount-preset">$15</span>
                        <span class="price-original-preset">$20</span>
                        <span style="font-size: 18px; color: #d1d1d1;">USD</span>
                    </div>

                    <ul class="pricing-includes">
                        <li><i class="bi bi-check-circle-fill"></i> Todo lo de la Opción 1</li>
                        <li><i class="bi bi-check-circle-fill"></i> <strong>BONUS:</strong> Drumkit EXCLUSIVO de OFFSZN</li>
                        <li><i class="bi bi-check-circle-fill"></i> <strong>BONUS:</strong> $5 Gift Card OFFSZN</li>
                        <li><i class="bi bi-check-circle-fill"></i> Samples Royalty-Free 100%</li>
                        <li><i class="bi bi-check-circle-fill"></i> Optimizado para Trap, Drill, Plugg, Urbano, Reggaetón, R&B</li>
                        <li style="color: #0cbc87;"><i class="bi bi-lightning-charge-fill"></i> <strong>Oferta por Tiempo Limitado</strong></li>
                    </ul>

                    <a href="#" class="btn btn-block">
                        Obtener Edición de Lanzamiento
                        <i class="bi bi-arrow-right"></i>
                    </a>

                    <div class="countdown-timer-preset">
                        <h4 style="font-size: 12px; text-transform: uppercase; letter-spacing: 1px; color: #b794f6; margin-bottom: 20px;">
                            <i class="bi bi-clock"></i> La Oferta Termina En:
                        </h4>
                        <div class="timer-display">
                            <div class="timer-block">
                                <span class="timer-number">05</span>
                                <span class="timer-label">Días</span>
                            </div>
                            <div class="timer-block">
                                <span class="timer-number">12</span>
                                <span class="timer-label">Horas</span>
                            </div>
                            <div class="timer-block">
                                <span class="timer-number">34</span>
                                <span class="timer-label">Minutos</span>
                            </div>
                            <div class="timer-block">
                                <span class="timer-number">56</span>
                                <span class="timer-label">Segundos</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ==================== BONUS SECTION ==================== -->
    <section class="bonus-section">
        <div class="section-header-center">
            <span class="section-eyebrow">Bonificaciones Exclusivas</span>
            <h2>Incluidos en la Edición de Lanzamiento</h2>
            <p>Obtén más valor con estos bonos exclusivos disponibles por tiempo limitado</p>
        </div>

        <div class="bonus-grid">
            <div class="bonus-card">
                <span class="bonus-badge-card">GRATIS</span>
                <span class="bonus-value">(Valor: $10)</span>
                <h3><i class="bi bi-music-note-beamed"></i> Drumkit EXCLUSIVO OFFSZN</h3>
                <div class="bonus-description">
                    <p>Drumkit premium con samples únicos diseñados específicamente para trap, drill y géneros urbanos. Incluye 808s, snares, hi-hats, percs y más. 100% royalty-free.</p>
                </div>
            </div>

            <div class="bonus-card">
                <span class="bonus-badge-card">GRATIS</span>
                <h3><i class="bi bi-gift-fill"></i> $5 OFFSZN Store Credit</h3>
                <div class="bonus-description">
                    <p>Recibe $5 USD en crédito para usar en futuros productos de OFFSZN. El crédito se envía automáticamente después de la compra y nunca expira.</p>
                    <p style="margin-top: 12px; font-size: 13px; color: #999;">
                        <i class="bi bi-info-circle"></i>
                        Cada pedido solo es elegible para recibir 1 crédito de la tienda OFFSZN
                    </p>
                </div>
            </div>

            <div class="bonus-card">
                <span class="bonus-badge-card">INCLUIDO</span>
                <h3><i class="bi bi-file-play-fill"></i> Video Tutorial Completo</h3>
                <div class="bonus-description">
                    <p>Tutorial paso a paso donde explicamos cada plugin, cada parámetro y cómo conseguir el mejor sonido. Aprende técnicas profesionales de mezcla vocal.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- ==================== FAQ SECTION ==================== -->
    <section class="faq-section">
        <div class="section-header-center">
            <span class="section-eyebrow">¿Tienes Preguntas?</span>
            <h2>Preguntas Frecuentes</h2>
            <p>Encuentra respuestas a las preguntas más comunes sobre el Preset Definitivo</p>
        </div>

        <div class="faq-container">
            <div class="faq-item-preset">
                <div class="faq-question">¿El preset funciona en cualquier versión de FL Studio?</div>
                <div class="faq-answer">
                    <p>Sí, el preset es compatible con FL Studio. Solo necesitas tener los plugins de WAVES instalados para usarlo correctamente. Funciona con versiones recientes de FL Studio 20 y superiores.</p>
                </div>
            </div>

            <div class="faq-item-preset">
                <div class="faq-question">¿Cómo recibo mi Gift Card de $5?</div>
                <div class="faq-answer">
                    <p>La Gift Card se enviará automáticamente a tu correo después de la compra junto con el preset. El crédito estará disponible en tu cuenta de OFFSZN para usar en futuros productos.</p>
                </div>
            </div>

            <div class="faq-item-preset">
                <div class="faq-question">¿La Gift Card expira?</div>
                <div class="faq-answer">
                    <p>No, tu crédito de $5 nunca expira. Puedes usarlo cuando quieras en la tienda OFFSZN para comprar cualquier producto disponible.</p>
                </div>
            </div>

            <div class="faq-item-preset">
                <div class="faq-question">¿El Drumkit Bonus es royalty-free para uso comercial?</div>
                <div class="faq-answer">
                    <p>Sí, todos los samples incluidos son 100% royalty-free y puedes usarlos en tus producciones comerciales sin problemas. No necesitas dar crédito ni pagar regalías adicionales.</p>
                </div>
            </div>

            <div class="faq-item-preset">
                <div class="faq-question">¿Necesito conocimientos avanzados para usar el preset?</div>
                <div class="faq-answer">
                    <p>No, el preset está diseñado para ser plug-and-play. Solo cárgalo en tu proyecto y obtendrás resultados profesionales al instante. El video tutorial te guiará paso a paso si quieres personalizar los parámetros.</p>
                </div>
            </div>

            <div class="faq-item-preset">
                <div class="faq-question">¿Qué plugins de WAVES necesito tener instalados?</div>
                <div class="faq-answer">
                    <p>El preset utiliza plugins estándar de WAVES que vienen en la mayoría de los bundles. En el video tutorial se especifican exactamente cuáles necesitas. Si no los tienes, puedes conseguirlos en el sitio oficial de WAVES.</p>
                </div>
            </div>

            <div class="faq-item-preset">
                <div class="faq-question">¿Puedo usar el preset en múltiples computadoras?</div>
                <div class="faq-answer">
                    <p>Sí, una vez que compres el preset, puedes instalarlo en todas tus computadoras personales sin restricciones. Es tuyo para siempre.</p>
                </div>
            </div>

            <div class="faq-item-preset">
                <div class="faq-question">¿Ofrecen reembolsos?</div>
                <div class="faq-answer">
                    <p>Debido a la naturaleza digital del producto, no ofrecemos reembolsos una vez descargado el preset. Sin embargo, estamos seguros de que quedarás satisfecho con la calidad profesional que obtendrás.</p>
                </div>
            </div>

            <div class="faq-item-preset">
                <div class="faq-question">¿El preset funciona con voces en cualquier idioma?</div>
                <div class="faq-answer">
                    <p>Sí, el preset está optimizado para procesar voces en general, independientemente del idioma. Funciona perfectamente con voces en español, inglés y cualquier otro idioma.</p>
                </div>
            </div>

            <div class="faq-item-preset">
                <div class="faq-question">¿Cuándo termina la oferta de lanzamiento?</div>
                <div class="faq-answer">
                    <p>La oferta de lanzamiento es por tiempo limitado. Una vez que termine, el precio volverá a $20 y los bonos exclusivos ya no estarán disponibles. ¡Aprovecha ahora mientras puedas!</p>
                </div>
            </div>
        </div>

        <div style="text-align: center; margin-top: 50px;">
            <p style="color: #d1d1d1; margin-bottom: 20px; font-size: 16px;">¿Tienes alguna otra pregunta?</p>
            <a href="mailto:offszn.studio@gmail.com" class="btn btn-secondary">
                <i class="bi bi-envelope"></i>
                Contáctanos
            </a>
        </div>
    </section>

    <!-- ==================== REVIEWS SECTION ==================== -->
    <section class="reviews-section">
        <div class="section-header-center">
            <span class="section-eyebrow">Lo Que Dicen Nuestros Clientes</span>
            <h2>Reseñas del Preset Definitivo</h2>
            <div class="overall-rating">
                <div class="rating-stars">
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                </div>
                <span class="rating-count">5.0 de 5 estrellas (basado en 10 reseñas)</span>
            </div>
        </div>

        <div class="reviews-grid">
            <div class="review-card">
                <div class="review-header">
                    <div class="reviewer-avatar">MC</div>
                    <div class="reviewer-info">
                        <h4>Miguel Castro</h4>
                        <div class="review-date">15 de diciembre, 2024</div>
                    </div>
                </div>
                <div class="review-rating">
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                </div>
                <div class="review-title">Mis voces nunca sonaron tan profesionales</div>
                <p class="review-text">Compré el preset hace una semana y ya lo usé en 3 tracks diferentes. La diferencia es brutal, mis voces ahora tienen esa claridad y presencia que siempre busqué. El tutorial está súper completo y fácil de seguir. Totalmente recomendado para trap y drill.</p>
            </div>

            <div class="review-card">
                <div class="review-header">
                    <div class="reviewer-avatar">AR</div>
                    <div class="reviewer-info">
                        <h4>Andrea Rojas</h4>
                        <div class="review-date">16 de diciembre, 2024</div>
                    </div>
                </div>
                <div class="review-rating">
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                </div>
                <div class="review-title">Calidad de estudio al instante</div>
                <p class="review-text">Soy productora de reggaetón y este preset me ahorró horas de trabajo. Antes tardaba un montón en conseguir ese sonido limpio y balanceado, ahora solo cargo el preset y listo. El drumkit bonus también está increíble, los samples son únicos.</p>
            </div>

            <div class="review-card">
                <div class="review-header">
                    <div class="reviewer-avatar">JL</div>
                    <div class="reviewer-info">
                        <h4>Javier Luna</h4>
                        <div class="review-date">17 de diciembre, 2024</div>
                    </div>
                </div>
                <div class="review-rating">
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                </div>
                <div class="review-title">Perfecto para principiantes y profesionales</div>
                <p class="review-text">Llevo produciendo 2 años y siempre tuve problemas con la mezcla vocal. Este preset me dio ese empujón que necesitaba. Funciona perfecto en FL Studio y los plugins de WAVES hacen magia. La gift card de $5 fue un plus genial, ya compré otro pack.</p>
            </div>

            <div class="review-card">
                <div class="review-header">
                    <div class="reviewer-avatar">DS</div>
                    <div class="reviewer-info">
                        <h4>Daniel Suárez</h4>
                        <div class="review-date">18 de diciembre, 2024</div>
                    </div>
                </div>
                <div class="review-rating">
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                </div>
                <div class="review-title">El mejor preset que he comprado</div>
                <p class="review-text">Trabajo en un estudio pequeño y este preset se convirtió en mi herramienta principal. Mis clientes notan la diferencia inmediatamente. La compresión está perfecta, ni muy suave ni muy agresiva. Vale cada centavo, especialmente con los bonos de lanzamiento.</p>
            </div>

            <div class="review-card">
                <div class="review-header">
                    <div class="reviewer-avatar">LM</div>
                    <div class="reviewer-info">
                        <h4>Laura Méndez</h4>
                        <div class="review-date">19 de diciembre, 2024</div>
                    </div>
                </div>
                <div class="review-rating">
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                </div>
                <div class="review-title">Increíble para R&B y soul</div>
                <p class="review-text">Produzco R&B y soul principalmente, y este preset le dio a mis voces esa calidez y profundidad que buscaba. La ecualización está súper bien pensada y el video tutorial explica cada detalle. Lo mejor es que puedo ajustarlo a mi gusto si quiero experimentar.</p>
            </div>

            <div class="review-card">
                <div class="review-header">
                    <div class="reviewer-avatar">CT</div>
                    <div class="reviewer-info">
                        <h4>Carlos Torres</h4>
                        <div class="review-date">20 de diciembre, 2024</div>
                    </div>
                </div>
                <div class="review-rating">
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                    <i class="bi bi-star-fill"></i>
                </div>
                <div class="review-title">Resultados instantáneos</div>
                <p class="review-text">Lo que más me gustó es lo rápido que obtuve resultados profesionales. Antes perdía horas ajustando compresores y EQs, ahora en 5 minutos tengo mis voces listas. El preset para el BUS de voces también es excelente para darle cohesión a todo.</p>
            </div>
        </div>
    </section>

    <!-- ==================== FOOTER ==================== -->
    <footer class="footer">
        <div class="footer-content">
            <p>© 2025 OFFSZN</p>
            <ul>
                <li><a href="politica-de-privacidad.html" target="_blank" rel="noopener noreferrer">Política de Privacidad</a></li>
                <li><a href="terminos-de-uso.html" target="_blank" rel="noopener noreferrer">Términos de Uso</a></li>
                <li><a href="terminos-de-servicio.html" target="_blank" rel="noopener noreferrer">Términos de Servicio</a></li>
                <li><a href="contacto.html">Contacto</a></li>
            </ul>
            <div class="footer-social">
                <a href="https://wa.link/t89kep" target="_blank" rel="noopener noreferrer" title="WhatsApp">
                    <i class="bi bi-whatsapp"></i>
                </a>
                <a href="https://www.instagram.com/willieinspired/" target="_blank" rel="noopener noreferrer" title="Instagram">
                    <i class="bi bi-instagram"></i>
                </a>
                <a href="https://www.youtube.com/@willieinspired" target="_blank" rel="noopener noreferrer" title="YouTube">
                    <i class="bi bi-youtube"></i>
                </a>
            </div>
        </div>
    </footer>

    <!-- ==================== SCRIPTS ==================== -->
    <script src="../script/menu.js"></script>

    <script>
        // ========================================
        // LENIS SMOOTH SCROLL
        // ========================================
        const lenis = new Lenis({
            duration: 1.2,
            easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
            orientation: 'vertical',
            gestureOrientation: 'vertical',
            smoothWheel: true,
            wheelMultiplier: 1,
            smoothTouch: false,
            touchMultiplier: 2,
            infinite: false,
        });

        function raf(time) {
            lenis.raf(time);
            requestAnimationFrame(raf);
        }

        requestAnimationFrame(raf);

        // ========================================
        // GSAP + SCROLLTRIGGER ANIMATIONS
        // ========================================
        gsap.registerPlugin(ScrollTrigger);

        // Actualizar ScrollTrigger cuando Lenis hace scroll
        lenis.on('scroll', ScrollTrigger.update);

        // Sincronizar GSAP con Lenis
        gsap.ticker.lagSmoothing(0);

        // ===== HERO ANIMATIONS CINEMATOGRÁFICAS =====
        gsap.from('.preset-badge', {
            opacity: 0,
            y: -30,
            duration: 0.8,
            ease: 'power3.out',
            delay: 0.2
        });

        gsap.from('.preset-detail-title', {
            opacity: 0,
            y: 50,
            duration: 1,
            delay: 0.4,
            ease: 'power3.out'
        });

        gsap.from('.preset-detail-subtitle', {
            opacity: 0,
            y: 30,
            duration: 0.9,
            delay: 0.6,
            ease: 'power2.out'
        });

        gsap.from('.hero-cta-group .btn', {
            opacity: 0,
            y: 30,
            duration: 0.7,
            delay: 0.8,
            stagger: 0.15,
            ease: 'back.out(1.7)'
        });

        gsap.from('.preset-image-placeholder', {
            opacity: 0,
            scale: 0.85,
            rotateY: -15,
            duration: 1.2,
            delay: 0.5,
            ease: 'power3.out'
        });

        // ===== PARALLAX EFFECT EN IMAGEN DEL HERO =====
        gsap.to('.preset-image-placeholder', {
            scrollTrigger: {
                trigger: '.preset-detail-hero',
                start: 'top top',
                end: 'bottom top',
                scrub: 1.5
            },
            y: 80,
            rotateY: 5,
            ease: 'none'
        });

        // ===== SECTION HEADERS CON REVEAL =====
        gsap.utils.toArray('.section-header-center').forEach(header => {
            gsap.from(header.querySelectorAll('.section-eyebrow, h2, p'), {
                scrollTrigger: {
                    trigger: header,
                    start: 'top 85%',
                    toggleActions: 'play none none none'
                },
                opacity: 0,
                y: 40,
                duration: 0.9,
                stagger: 0.15,
                ease: 'power3.out'
            });
        });

        // ===== VIDEO CONTAINER CON SCALE =====
        gsap.from('.video-container', {
            scrollTrigger: {
                trigger: '.video-container',
                start: 'top 80%',
                toggleActions: 'play none none none'
            },
            opacity: 0,
            scale: 0.92,
            duration: 1.2,
            ease: 'power3.out'
        });

        // ===== BEFORE/AFTER CARDS CON STAGGER =====
        gsap.from('.ba-card', {
            scrollTrigger: {
                trigger: '.ba-grid',
                start: 'top 75%',
                toggleActions: 'play none none none'
            },
            opacity: 0,
            y: 60,
            rotateX: 10,
            duration: 1,
            stagger: 0.25,
            ease: 'power3.out'
        });

        // ===== FEATURES CARDS CON EFECTO 3D =====
        gsap.from('.feature-card', {
            scrollTrigger: {
                trigger: '.features-grid',
                start: 'top 75%',
                toggleActions: 'play none none none'
            },
            opacity: 0,
            y: 60,
            rotateY: -8,
            duration: 0.9,
            stagger: 0.12,
            ease: 'power3.out'
        });

        // ===== PRICING CARDS CON BOUNCE =====
        gsap.from('.pricing-card-preset', {
            scrollTrigger: {
                trigger: '.pricing-options',
                start: 'top 75%',
                toggleActions: 'play none none none'
            },
            opacity: 0,
            y: 70,
            scale: 0.9,
            duration: 1,
            stagger: 0.2,
            ease: 'back.out(1.4)'
        });

        // ===== BONUS CARDS =====
        gsap.from('.bonus-card', {
            scrollTrigger: {
                trigger: '.bonus-grid',
                start: 'top 75%',
                toggleActions: 'play none none none'
            },
            opacity: 0,
            y: 60,
            duration: 0.9,
            stagger: 0.18,
            ease: 'power3.out'
        });

        // ===== FAQ ITEMS CON SLIDE =====
        gsap.from('.faq-item-preset', {
            scrollTrigger: {
                trigger: '.faq-container',
                start: 'top 80%',
                toggleActions: 'play none none none'
            },
            opacity: 0,
            x: -50,
            duration: 0.7,
            stagger: 0.08,
            ease: 'power2.out'
        });

        // ===== REVIEWS CARDS =====
        gsap.from('.review-card', {
            scrollTrigger: {
                trigger: '.reviews-grid',
                start: 'top 75%',
                toggleActions: 'play none none none'
            },
            opacity: 0,
            y: 50,
            scale: 0.95,
            duration: 0.8,
            stagger: 0.1,
            ease: 'power3.out'
        });

        // ========================================
        // FAQ ACCORDION
        // ========================================
        document.querySelectorAll('.faq-item-preset').forEach(item => {
            item.querySelector('.faq-question').addEventListener('click', () => {
                const isActive = item.classList.contains('active');

                // Cerrar todos los items
                document.querySelectorAll('.faq-item-preset').forEach(i => {
                    i.classList.remove('active');
                });

                // Abrir el clickeado si no estaba activo
                if (!isActive) {
                    item.classList.add('active');
                }
            });
        });

        // ========================================
        // SMOOTH SCROLL PARA ENLACES INTERNOS
        // ========================================
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const target = document.querySelector(targetId);
                
                if (target) {
                    lenis.scrollTo(target, {
                        offset: -80,
                        duration: 1.5,
                        easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t))
                    });
                }
            });
        });

        // ========================================
        // HOVER EFFECTS MEJORADOS
        // ========================================
        
        // Magnetic effect en botones principales
        const buttons = document.querySelectorAll('.btn');
        
        buttons.forEach(btn => {
            btn.addEventListener('mouseenter', function() {
                gsap.to(this, {
                    scale: 1.05,
                    duration: 0.4,
                    ease: 'power2.out'
                });
            });
            
            btn.addEventListener('mouseleave', function() {
                gsap.to(this, {
                    scale: 1,
                    duration: 0.4,
                    ease: 'power2.out'
                });
            });

            // Efecto de seguimiento del mouse
            btn.addEventListener('mousemove', function(e) {
                const rect = this.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                
                gsap.to(this, {
                    '--x': x + 'px',
                    '--y': y + 'px',
                    duration: 0.3
                });
            });
        });

        // Hover effect en cards con tilt suave
        const cards = document.querySelectorAll('.feature-card, .ba-card, .bonus-card, .review-card');
        
        cards.forEach(card => {
            card.addEventListener('mouseenter', function() {
                gsap.to(this, {
                    y: -12,
                    duration: 0.4,
                    ease: 'power2.out'
                });
            });
            
            card.addEventListener('mouseleave', function() {
                gsap.to(this, {
                    y: 0,
                    rotateX: 0,
                    rotateY: 0,
                    duration: 0.4,
                    ease: 'power2.out'
                });
            });

            // Efecto 3D tilt
            card.addEventListener('mousemove', function(e) {
                const rect = this.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                
                const centerX = rect.width / 2;
                const centerY = rect.height / 2;
                
                const rotateX = (y - centerY) / 20;
                const rotateY = (centerX - x) / 20;
                
                gsap.to(this, {
                    rotateX: rotateX,
                    rotateY: rotateY,
                    duration: 0.3,
                    ease: 'power2.out',
                    transformPerspective: 1000
                });
            });
        });

        // ========================================
        // COUNTDOWN TIMER (PLACEHOLDER)
        // ========================================
        function initCountdown() {
            // Fecha objetivo (5 días desde ahora para demo)
            const endDate = new Date();
            endDate.setDate(endDate.getDate() + 5);
            
            function updateTimer() {
                const now = new Date();
                const diff = endDate - now;
                
                if (diff <= 0) {
                    clearInterval(timerInterval);
                    return;
                }
                
                const days = Math.floor(diff / (1000 * 60 * 60 * 24));
                const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((diff % (1000 * 60)) / 1000);
                
                const timerNumbers = document.querySelectorAll('.timer-number');
                if (timerNumbers.length >= 4) {
                    timerNumbers[0].textContent = String(days).padStart(2, '0');
                    timerNumbers[1].textContent = String(hours).padStart(2, '0');
                    timerNumbers[2].textContent = String(minutes).padStart(2, '0');
                    timerNumbers[3].textContent = String(seconds).padStart(2, '0');
                }
            }
            
            updateTimer();
            const timerInterval = setInterval(updateTimer, 1000);
        }

        // ========================================
        // INIT
        // ========================================
        document.addEventListener('DOMContentLoaded', function () {
            initCountdown();
            
            // Smooth scroll al cargar página con hash
            if (window.location.hash) {
                setTimeout(() => {
                    const target = document.querySelector(window.location.hash);
                    if (target) {
                        lenis.scrollTo(target, {
                            offset: -80,
                            duration: 0
                        });
                    }
                }, 100);
            }

            // Animación de entrada del navbar
            gsap.from('.navbar', {
                y: -100,
                opacity: 0,
                duration: 0.8,
                ease: 'power3.out'
            });
        });

        // Prevenir comportamiento por defecto del scroll en enlaces con #
        window.addEventListener('load', () => {
            if (window.location.hash) {
                history.replaceState(null, null, ' ');
            }
        });

        // ========================================
        // CURSOR PERSONALIZADO (OPCIONAL)
        // ========================================
        const cursor = document.createElement('div');
        cursor.style.cssText = `
            width: 20px;
            height: 20px;
            border: 2px solid #7209b7;
            border-radius: 50%;
            position: fixed;
            pointer-events: none;
            z-index: 9999;
            transition: transform 0.2s ease;
            mix-blend-mode: difference;
            display: none;
        `;
        document.body.appendChild(cursor);

        document.addEventListener('mousemove', (e) => {
            cursor.style.display = 'block';
            gsap.to(cursor, {
                x: e.clientX - 10,
                y: e.clientY - 10,
                duration: 0.3,
                ease: 'power2.out'
            });
        });

        document.querySelectorAll('a, button, .card').forEach(el => {
            el.addEventListener('mouseenter', () => {
                gsap.to(cursor, {
                    scale: 1.5,
                    duration: 0.3
                });
            });
            
            el.addEventListener('mouseleave', () => {
                gsap.to(cursor, {
                    scale: 1,
                    duration: 0.3
                });
            });
        });
    </script>
</body>

</html>
